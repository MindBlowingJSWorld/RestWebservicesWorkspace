//this is the controller for the screen.
(function(){
    'use strict';
    var homeController = function($scope, $location) {
        //Add Scope Variables
    };
    
    angular.module(ccs.shell.module.name)
        .controller('homeController',
             ['$scope', '$location', homeController]);

})();