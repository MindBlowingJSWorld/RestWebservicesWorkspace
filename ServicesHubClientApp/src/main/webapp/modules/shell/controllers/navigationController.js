//this is the controller for the screen.
(function () {
    'use strict';
    var navigationController = function ($scope, $location) {
        //add all scope variables and methods we want to handle here
    };

    angular.module(ccs.shell.module.name)
        .controller('navigationController',
        ['$scope', '$location', navigationController]);

})();